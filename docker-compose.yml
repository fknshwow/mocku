version: '3.8'

services:
  mocku-web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      # Mount the mocks directory from the project structure
      - ./src/Mocku.Web/mocks:/app/mocks:ro
    restart: unless-stopped